---
pre-push:
  commands:
    packages-audit:
      tags: frontend security
      run: pnpm audit

pre-commit:
  parallel: true
  commands:
    eslint:
      glob: '*.{js,ts}'
      run: pnpm eslint {staged_files}
    tests:
      glob: '*.{js,ts}'
      run: jest --findRelatedTests {staged_files}
